#Calculator

calculator built with HTML, CSS, and JavaScript, featuring a glassmorphism-inspired UI, smooth interactions, and full keyboard support.

🧠 How It Works
The calculator uses a simple state system:
display → current number shown
previousValue → stored value before an operation
operation → selected operator (+, -, ×, ÷)
newNumber → flag to reset display on next input
